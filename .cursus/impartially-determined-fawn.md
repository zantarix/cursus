+++
"@zantarix/cursus" = "minor"
+++

Verifies release downloads against a co-located Sigstore attestation bundle published as a `<binary>.sigstore.json` release asset, so verification is token-free and no longer depends on the rate-limited GitHub attestations API.
