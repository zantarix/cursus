# Changesets

When creating changesets or otherwise interacting with `cursus` as a CLI tool in this repo, you should run `cursus` by simply running `cargo run --` instead to build and run the currently in-development version of cursus. `cursus` is not available as a standalone binary in this repository.
